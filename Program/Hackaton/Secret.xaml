<Window x:Class="Hackaton.Secret"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Secret" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2C3E50" Padding="15" CornerRadius="5" Margin="0,0,0,20">
            <TextBlock Text="Управление секретом" 
                      FontSize="24" 
                      FontWeight="Bold" 
                      Foreground="White" 
                      HorizontalAlignment="Center"/>
        </Border>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10">
            <TextBox x:Name="SearchTextBox" Width="200" Height="25"/>
            <Button x:Name="RefreshBtn" Content="поиск" Margin="10,0,0,0" Background="#1ABC9C"/>
            <Button x:Name="AddSecretBtn" Content="Добавить секрет" Margin="10,0,0,0" Click="AddSecretBtn_Click" Background="#3498DB"/>
            <Button x:Name="RequestAccessBtn" Content="Подать заявку" Margin="10,0,0,0" Background="#2980B9" Click="RequestAccessBtn_Click"/>
        </StackPanel>

        <DataGrid x:Name="SecretsGrid" Grid.Row="2" Margin="10"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

        <StatusBar Grid.Row="3">
            <TextBlock x:Name="StatusText" Text="Готов"/>
        </StatusBar>
    </Grid>
</Window>